# 코딩 취향 (Sayne)

> AI가 멋대로 수정하는 문서가 아니다. 이 문서에 추가·수정을 명시적으로 부탁했을 때만 고친다.

내가 중요시하는 습관은 SSOT · YAGNI · 캡슐화 · SOLID다.

## 작업 방식

- 별도 지시가 없는 한 시킨 것만, 보수적으로, 안전하게 작성
- 설계 갈림길이 있으면 마음대로 고르지 말고 물어본다
- 어떤 스크립트가 재사용 가치가 있을 때 역으로 제의한다
- R3 · UniRx · DOTween 사용을 선호하니, 쓸 만한 곳이 보이면 역으로 제의한다
- 아래 네이밍·코드·Unity 관습을 지킬 것

## 대화

- 간결하게. 설명 장황하게 하지 말 것
- 묻기만 한 건 실행하지 말 것. 묻는 건 진짜 궁금해서 묻는 거지 추궁이나 특정 행동을 바라는 비꼬기가 아님

## 네이밍

- 네임스페이스는 프로젝트명이 아니라 항상 `Sayne`
- private 필드는 `_camelCase`. 전부 닫고 시작하고, 실제 호출부가 생겼을 때만 프로퍼티로 연다
- 캡슐화가 기본. 단, 밖에서 읽고 쓰는 게 의도상 확실한 값은 `{ get; set; }` 자동 프로퍼티로 선언해도 된다 — 억지로 필드+프로퍼티 쌍을 만들지 않는다
- bool은 `is` / `has` / `can` 접두사 — `_isAlive`
- 런타임에 변하는 현재값에는 `current`를 붙여 설정값과 구분한다 — `_currentHP` vs `_maxHP`
- 초기화 메서드는 `Initialize`가 아니라 `Init`
- 조회 메서드는 `Get~` — `GetWeapon(id)`, `Get(wave)`. `~Of(id)` / `Of(id)` 같은 전치사식 이름은 안 씀
- 비동기 메서드는 `Async` 접미사 — `MainLogicAsync`
- 두 글자 약어는 둘 다 대문자 — `HP`, `MaxHP`
- 상수는 PascalCase. `UPPER_CASE` 안 씀
- **enum 이름에는 `Type` 접미사를 붙인다** — `DebuffType`, `WeaponType`. 접미사가 없으면 "종류 이름표"인지 "실제 한 건"인지 구분이 안 되고, 나중에 그 종류에 지속시간 같은 값이 붙은 타입을 만들 때 쓸 이름이 사라진다. 실제 한 건을 담는 타입이 접미사 없는 이름을 가져간다 — `Debuff { DebuffType Type; float Seconds; }`
- enum 의 목록·표시명을 들고 있는 짝꿍 static class 는 복수형으로 같은 파일에 둔다 — `DebuffTypes.All`, `WeaponTypes.DisplayName()`
- 헝가리안 표기 안 씀
- `Button`은 `button`이 아니라 `btn`으로 줄인다 — `_rerollBtn`
- 임시·테스트값에는 `Test` 접두사 — `TestHeroMaxHP`

## R3

- 상태는 `ReactiveProperty`, 일반 변수 규칙 그대로 — `IsAlive`, `CurrentHP`
- 사건은 과거형 — `Attacked`, `Damaged`, `Spawned`
- 스트림에 `On~` / `~Property` / `Rp` 안 붙임
- 구독 해제는 MonoBehaviour 안에서는 `.AddTo(this)` — 남의 수명에 걸 땐 `.AddTo(hero)`. 순수 클래스인 매니저는 `AddTo` 를 못 쓰니 `.RegisterTo(LifeToken)`
- **작은 위젯은 매니저를 받지 않는다.** 볼 값은 `Observable<T>` 로 받는다 — `CurrencyWidget.Init(Observable<long> amount)`. 위젯은 그게 어느 매니저의 무엇인지 모르고, 어느 위젯이 무엇을 볼지는 패널이 정한다
- **버튼이 눌렸을 때 할 일은 `Init` 에서 `Action` 으로 주입받는 게 기본이다** — `QuestWidget.Init(..., Action onClaim)`. `Init` 한 번으로 위젯이 완성되고 구독 수명도 위젯이 챙긴다
- 예외로 `Clicked` 스트림을 밖으로 내보내는 건 두 경우뿐이다 — 처리하는 쪽이 `Init` 하는 쪽보다 바깥에 있어 Action 을 받아 넘기기만 하는 중간 다리가 생길 때(스킬 버튼 → BattlePanel → HeroControlManager), 그리고 클릭 하나를 여러 곳이 들어야 할 때

## 문제 해결

- **증상이 아니라 원인을 고친다.** 예외가 난 자리에 방어 코드를 붙이지 말고, 그 상태가 왜 생겼는지를 찾아 거기서 끊는다
- 고치기 전에 "이게 원인인가, 증상인가"를 한 번 묻는다. 플래그·null 체크·try-catch 를 더하고 있다면 대개 증상을 덮는 중이다
- 수명이 끝난 매니저를 누가 부르고 있다면, 매니저에 "나 죽었다" 표식을 다는 게 아니라 **부르는 쪽이 자기 수명을 지키게** 한다

## 코드

- 상황에 따라 `var` 써도 됨
- **switch 는 갈래를 전부 명시한다.** 마지막 하나를 `_ =>` 로 받아 실제 값을 넣지 말 것 — `_ => "이동속도"` 처럼 쓰면 enum 에 항목이 늘었을 때 그게 조용히 "이동속도"가 되어 버린다. 남은 갈래는 `_ => throw new ArgumentOutOfRangeException(nameof(type), type, null)` 로 막아서, 빠뜨린 경우가 값이 아니라 에러로 드러나게 한다
- **하위 클래스에서 상위 클래스의 함수를 부를 땐 반드시 `base.` 를 붙인다** — `base.Init(plan.Body, plan.Combat, equipment)`. 같은 이름의 오버로드가 있으면 자기 것을 부르는지 부모 것을 부르는지 읽는 쪽이 헷갈린다
- 중복은 두 번째가 아니라 세 번째에 뽑는다
- 주석은 최소화한다
- 주석은 용어가 쉽냐 어렵냐의 문제가 아니라 핵심을 적는 것 — 이 코드가 왜 존재하고 뭘 보장하는지만 적는다

## Unity

- 생명주기 함수(`Awake` / `Update` 등)는 한 줄이어도 중괄호 블록. `=>` 금지
- `SerializeField`는 바인딩에만 쓰는 걸 선호한다. 밸런스 수치나 설정값을 인스펙터에 노출하는 건 선호하지 않는다
- 런타임 어드레서블 API 호출은 `AssetManagerBase` 안에서만 한다. 다른 곳에서 에셋이 필요하면 에셋 매니저를 만들어 주입받는다 (에디터 툴은 예외)
- `SayneAssets` 는 다른 프로젝트에 폴더째 들고 가서 그대로 쓰는 부품이다. **부품이 쓰는 그림·폰트 같은 자산은 그 부품 폴더 안에 함께 둔다** — 게임 쪽 자산을 빌려 쓰면 옮겨갔을 때 스크립트만 남는다. 게임 전용 그림으로 갈아끼울 거라도 **샘플을 넣어 두어 혼자서 서게** 만든다 (예: `UI/Joystick/Sprites`, `UI/Speech/Sprites`). 진짜 여러 부품이 나눠 쓰는 것만 `UI/Sprites` 같은 공용 자리에 둔다
- `SayneAssets` 에 같이 넣는 예시 프리팹은 이름에 **`.example` 을 붙인다** (예: `UI/DamageText/DamageText.example.prefab`). 게임이 실제로 쓰는 프리팹은 게임 폴더에 따로 두고, 예시는 부품이 혼자 서는지 보여 주는 견본일 뿐이라는 게 이름에서 보여야 한다
- **각 개체(캐릭터·아이템 등)는 자기 것만으로 완결되게 만든다 (자기완결성, Self-contained).** 지금 겉보기에 같더라도 다른 개체의 리소스·설정을 참조로 빌려 쓰지 않는다 — 필요하면 복사해서 자기 폴더에 자기 이름으로 둔다. 중복은 허용한다: 잘못 공유하느니 중복이 낫다. 지키려는 건 두 가지다 — 한 개체를 볼 때 그 폴더만 보면 다 있어야 하고, 한 개체를 고칠 때 다른 개체가 따라 바뀌면 안 된다. 공유가 편해 보여도 원리가 일관된 쪽을 고른다 (예: 오우거 족장 초상화는 오우거 것을 가리키지 않고 `OgreBoss/OgreBossPortrait.png` 로 복사해 둔다)
- 폴더·자산 이름은 **쓰임새가 아니라 정체**로 짓는다. `UI/Tutorial` 이 아니라 `UI/Speech` — 튜토리얼에서 쓸지는 그다음 사정이다
- 폴더는 **형식이 아니라 기능 단위로 묶는다.** 기준은 "이 폴더만 통째로 들어내도 그 기능이 그대로 따라 나오나" 다 — 에셋 하나를 통째로 내려받은 것처럼. 장비창·장비 위젯·장비 프리팹은 형식이 제각각이어도 `Equipment/` 아래 같이 산다. `UI/Widgets` 에 온 게임의 위젯을 모으고 `UI/Popup` 에 온 게임의 창을 모으면, 장비를 들어낼 때 세 폴더를 뒤져야 한다
- 형식으로 묶는 것 자체가 금지는 아니다 — **기능 안에서 한 번 더 나눌 때**는 괜찮다 (`Equipment/UI`, `Drops/Art`). 순서가 뒤집히면 안 된다는 뜻이다. 어느 기능에도 매이지 않은 공용 부품만 `UI/Widgets` 같은 형식 폴더에 남는다
- **예외: 사운드는 `Game/Sounds/` 한 곳에 모은다.** 그 안에서 기능별로 나눈다 (`Sounds/Weapon/BowShot.wav`). 소리는 한데 모아 두고 비교하며 관리하는 게 낫다는 판단이다
- 매니저가 프리팹을 생성해 관리하면 **회수도 반드시 그 매니저의 함수로** 한다. 밖에서 `Destroy` 하거나 직접 끄지 않는다 — 목록·구독·정리 같은 내부 사정은 매니저가 쥐고, 밖으로는 함수만 드러난다 (예: `EnemyManager.SpawnEnemy` / `DestroyEnemy`)
- **프리팹은 생성하는 그 자리에서 `Init` 한다.** `Instantiate` 바로 다음에 따라갈 대상·카메라·그림처럼 필요한 걸 전부 넘긴다. 미리 만들어 두었다가 나중에 붙이거나 채우지 않는다 — 쓸 때 만들고, 다 쓰면 만든 쪽이 치운다 (예: 월드 말풍선은 말할 때 만들어 `Init` 으로 머리 위를 따라다니게 하고, 말이 끝나면 매니저가 치운다)
- 만들어진 물건은 자기 수명을 스스로 끝내지 않는다. 대상이 사라졌는지 살피다 자살하는 대신, **만든 매니저가 치운다** — 수명의 주인은 하나다
- `GameManager` 는 매니저의 생성·주입·Init/Release 만 한다. 구독·배선·틱 같은 로직은 GameManager 에 쓰지 않는다 — 그런 정책이 생기면 담당 매니저를 따로 만든다 (예: `CameraDirector`). 각 매니저는 Init 후 스스로 돌아가고 스스로 정리한다
- **매니저는 할 줄 아는 것, 디렉터는 언제 무엇을 할지.** 매니저는 자기 목록·수명·기능을 쥔 창구다. 언제 왜 불리는지는 모르고, 부르면 할 뿐이다 (`EnemyManager`, `TutorialManager` = 대사를 틀 줄 안다). 디렉터는 여러 매니저의 상황을 구독하고 조건이 맞으면 그 창구들을 엮어 한 장면을 짜는 정책이다 (`TutorialDirector` = 첫 보스가 오면 대사를 띄운다, `UltimateDirector`, `DropDirector`, `CameraDirector`)
- 의존은 한 방향이다 — **디렉터는 매니저를 알지만 매니저는 디렉터를 모른다.** 그래서 GameManager 에서도 디렉터는 쓰는 매니저들보다 뒤에 태어난다. 새 코드가 "무엇을 할 줄 아나" 면 매니저에, "이런 상황이 오면 이걸 한다" 면 디렉터에 둔다. 매니저 안에 상황 구독을 넣지 않는다
- 에디터 메뉴는 전부 최상위 `★Sayne★` 아래에 한글 이름으로 모은다. Tools 나 Game 같은 밋밋한 경로에 숨기지 않는다
- 에디터 메뉴에는 **지금 돌려야 할 일**과 **계속 쓰는 도구**만 둔다. 메뉴만 보고 무엇을 해야 하는지 알 수 있어야 한다
- 계속 쓰는 도구(빌더·프리뷰 등)는 하위 메뉴에 그대로 둔다 — 도메인이 있으면 그 이름으로(`영웅/`, `장비/`, `적/`, `UI/`), 도메인 없이 작업마다 또 돌리게 되는 것은 `공통/` 으로 묶는다
- **최상위의 번호 매긴 항목은 "지금 돌려야 할 일" 뿐이다.** 번호는 고정 목차가 아니라 이번에 돌릴 순서다 — 할 일이 바뀌면 1부터 다시 매긴다
- 번호 항목에는 왜 돌려야 하는지를 괄호로 붙인다 — `1. 전투 HUD 빌드 (조이스틱 전용 스프라이트 반영)`. 메뉴만 보고 무엇을 왜 하는지 알 수 있어야 한다
- 다 돌린 작업은 즉시 최상위에서 내린다. 또 돌릴 일이 있는 것은 `공통/` 으로, 정말 한 번뿐인 것은 `MenuItem` 어트리뷰트만 떼고 코드는 남긴다. 최상위에 번호가 쌓여 있으면 안 된다

## 커밋

- 접두사는 `feat:` / `fix:` / `refactor:`
- 제목은 한글
